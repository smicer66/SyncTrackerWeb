<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify OTP</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-900 to-slate-800 px-4">
    <div class="w-full max-w-sm rounded-2xl shadow-xl bg-white p-5 sm:p-6 space-y-6">
      <div class="text-center space-y-1">
        <h1 class="text-xl sm:text-2xl font-semibold">Verify Your Email Address</h1>
        <p class="text-sm text-gray-500">Enter the 6-digit code sent to your mobile</p>
      </div>

      <form method="POST" action="/verify-otp" class="space-y-6">
        <div class="flex justify-between gap-2">
          <input maxlength="1" inputmode="numeric" pattern="[0-9]*" class="w-12 h-12 text-center text-lg border rounded-lg focus:outline-none focus:ring" />
          <input maxlength="1" inputmode="numeric" pattern="[0-9]*" class="w-12 h-12 text-center text-lg border rounded-lg focus:outline-none focus:ring" />
          <input maxlength="1" inputmode="numeric" pattern="[0-9]*" class="w-12 h-12 text-center text-lg border rounded-lg focus:outline-none focus:ring" />
          <input maxlength="1" inputmode="numeric" pattern="[0-9]*" class="w-12 h-12 text-center text-lg border rounded-lg focus:outline-none focus:ring" />
          <input maxlength="1" inputmode="numeric" pattern="[0-9]*" class="w-12 h-12 text-center text-lg border rounded-lg focus:outline-none focus:ring" />
          <input maxlength="1" inputmode="numeric" pattern="[0-9]*" class="w-12 h-12 text-center text-lg border rounded-lg focus:outline-none focus:ring" />
        </div>

        <button type="submit" class="w-full bg-slate-900 text-white py-2 rounded-xl hover:bg-slate-800 transition">
          Verify
        </button>
      </form>

      <div class="text-center text-sm text-gray-500">
        Didn’t receive the code?
        <a href="#" class="text-blue-600 hover:underline">Resend OTP</a>
      </div>
    </div>
  </body>
</html>